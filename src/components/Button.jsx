import React from 'react';
import styles from './button.module.css';

const Button = () => {
  return (
    <div className={styles}>
      <div className={styles.customButton}>LET'S START</div>
    </div>
  );
};

export default Button;
